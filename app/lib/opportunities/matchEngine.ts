export function getMatchScore(opportunityId: string, creatorId: string) {
  return {
    score: 87,
    reasons: [
      "Audience overlap with lifestyle niche",
      "High engagement history in similar campaigns",
    ],
  };
}
// app/lib/streatswitchengine.ts

// 🧠 Legacy / safety stub
// This file is intentionally minimal to prevent runtime crashes.

export function getStreamWeights() {
  return {
    ENGAGEMENT: 0.3,
    STABILITY: 0.25,
    DISCOVERY: 0.25,
    TREND: 0.2,
  };
}

export function getActiveStreamMode() {
  return "forYou";
}